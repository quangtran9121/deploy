// import React from 'react';
// import classNames from 'classnames/bind';
// import Auth from "../../Pages/Login/Auth"; // Đường dẫn tới component Auth
// import styles from './Header.module.scss'; // Thêm file scss riêng nếu cần

// const cx = classNames.bind(styles);

// function AuthPopup({ isAuthOpen, onClose }) {
//     if (!isAuthOpen) return null; // Nếu không mở pop-up thì không render

//     return (
//         <div className={cx('auth-popup')}>
//             <div className={cx('auth-popup-overlay')} onClick={onClose} />
//             <div className={cx('auth-popup-content')}>
//                 <Auth />
//                 <button className={cx('close-btn')} onClick={onClose}>X</button>
//             </div>
//         </div>
//     );
// }

// export default AuthPopup;
